# 0522 — Add days

Parse the fixed date `2026-06-15` with the **pendulum** library, then add 10 days using pendulum's duration arithmetic (`.add(days=10)`) and print the ISO date string. The result is computed by the library, not hardcoded.

## Run

    python3 main.py
