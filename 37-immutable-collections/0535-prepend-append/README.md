# 0535 — Prepend & append

Use the `pyrsistent` persistent collection library: starting from a `pvector([2, 3])`, prepend `1` (via concatenation) and append `4` with `.append(4)`. Each operation RETURNS A NEW vector instead of mutating in place, leaving the original list unchanged.

## Run

    python3 main.py
