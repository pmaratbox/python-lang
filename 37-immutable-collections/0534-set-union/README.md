# 0534 — Set union

Use the `pyrsistent` persistent collection library: build two `pset` sets `{1, 2, 3}` and `{3, 4, 5}` and call `.union()`, which RETURNS A NEW `pset` rather than mutating either operand. The result's elements are sorted before printing for deterministic output.

## Run

    python3 main.py
