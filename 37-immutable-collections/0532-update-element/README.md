# 0532 — Update an element

Use the `pyrsistent` persistent collection library: build a `pvector([1, 2, 3])` and call `.set(0, 99)`, which RETURNS A NEW vector with the element replaced rather than mutating in place. Printing the new vector then the original shows the original is unchanged.

## Run

    python3 main.py
