# 0069 — Fibonacci (Iterative)

Print the first seven Fibonacci numbers, computed iteratively (each the sum of the previous two): `0 1 1 2 3 5 8`. Two variables hold the last pair; `a, b = b, a + b` rolls them forward, collecting `a` before each step.

## Run

    python3 main.py
