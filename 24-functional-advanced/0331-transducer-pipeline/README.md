# 0331 — Transducer Pipeline

Compose map(+1) with filter(even) and run it over [1,2,3,4], printing `2 4`. Transducers wrap a reducing step, so map and filter compose into one pass with no intermediate lists.

## Run

    python3 main.py
