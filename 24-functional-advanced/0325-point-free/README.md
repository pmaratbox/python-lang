# 0325 — Point-Free Style

Express "sum of squares" point-free (compose map-square with sum) and apply it to [1,2,3], printing `14`. A tiny `compose` helper threads `map(square)` into `sum` with no explicit argument.

## Run

    python3 main.py
