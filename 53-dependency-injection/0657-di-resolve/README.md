# 0657 — Resolve a service

Register a `Greeter` service in a `dependency-injector` `DeclarativeContainer` (using a `providers.Factory`), then resolve it from the container and call `greet()`, which returns `hello`.

## Run

    python3 main.py
