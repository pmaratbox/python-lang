# 0142 — Countdown by Recursion

Recursively count down from 5 to 1, printing `5 4 3 2 1` on one line. A recursive call builds a list that `" ".join` renders as one space-separated line.

## Run

    python3 main.py
