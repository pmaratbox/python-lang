# 0338 — Return Min and Max

Return both the minimum and maximum of [4,1,7] from one function and print `1 7`. A function returns multiple values as a tuple, unpacked at the call site.

## Run

    python3 main.py
