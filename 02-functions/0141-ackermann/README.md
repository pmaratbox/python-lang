# 0141 — Ackermann Function

Compute the Ackermann function A(2,3) recursively and print `9`. Python's nested function calls express the doubly recursive definition directly.

## Run

    python3 main.py
