# 0158 — String Rotation Check

Check whether "cdab" is a rotation of "abcd", printing `yes`. b is a rotation of a iff they share length and b is a substring of a+a, tested with the `in` operator.

## Run

    python3 main.py
