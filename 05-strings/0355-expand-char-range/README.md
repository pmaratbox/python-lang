# 0355 — Expand Character Range

Expand the range spec "a-e" into the characters `abcde`. Iterate from the start char code to the end char code inclusive and build the string.

## Run

    python3 main.py
