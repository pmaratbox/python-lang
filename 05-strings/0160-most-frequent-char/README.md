# 0160 — Most Frequent Character

Find the most frequent character in "hello", printing `l`. collections.Counter tallies chars and most_common(1) returns the max (l appears twice).

## Run

    python3 main.py
