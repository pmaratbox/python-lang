# 0592 — Attribute value

Uses Python's standard-library `xml.etree.ElementTree` to parse a fixed catalog document and extract the `id` attribute of the first `<book>` element (`b1`) via `.get("id")`.

## Run

    python3 main.py
