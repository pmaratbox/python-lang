# 0595 — All attributes

Uses Python's standard-library `xml.etree.ElementTree` to parse a fixed catalog document and collect the `id` attribute of every `<book>` element, joining them with commas (`b1,b2`).

## Run

    python3 main.py
