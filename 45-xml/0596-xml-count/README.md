# 0596 — Count elements

Uses Python's standard-library `xml.etree.ElementTree` to parse a fixed catalog document and count its `<book>` elements with `findall("book")`, printing the count `2`.

## Run

    python3 main.py
