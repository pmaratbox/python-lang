# 0597 — Sum numeric children

Uses Python's standard-library `xml.etree.ElementTree` to parse a fixed catalog document, read each book's `<price>` element text as an integer, and print the sum of all prices (`30 + 45` → `75`).

## Run

    python3 main.py
