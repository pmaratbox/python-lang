# 0594 — Find all elements

Uses Python's standard-library `xml.etree.ElementTree` to parse a fixed catalog document, find **all** `<book>` elements with `findall`, take each book's `<title>` text, and join them with commas (`Go,Rust`).

## Run

    python3 main.py
