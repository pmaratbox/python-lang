# 0591 — Element text

Uses Python's standard-library `xml.etree.ElementTree` to parse a fixed catalog document and extract the text of the first book's `<title>` element (`Go`).

## Run

    python3 main.py
