# 0593 — Nested element

Uses Python's standard-library `xml.etree.ElementTree` to parse a fixed catalog document and read the text of the first book's nested `<price>` element (`30`).

## Run

    python3 main.py
