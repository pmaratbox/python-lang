# 0154 — Polymorphic Area

Sum the areas of a Rectangle(2,3)=6 and a Triangle(base=4,height=4)=8 through a common Shape interface, printing `total area: 14`. An `abc.ABC` base with `@abstractmethod` defines the shared interface.

## Run

    python3 main.py
