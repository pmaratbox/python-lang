# 0152 — toString Override

Override the string conversion of a Point(1,2) so it prints `Point(1, 2)`. Defining `__str__` is how a class controls its print() representation.

## Run

    python3 main.py
