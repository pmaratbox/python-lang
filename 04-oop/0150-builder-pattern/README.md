# 0150 — Builder Pattern

Use a fluent builder to construct a Pizza of size M with cheese, and print `Pizza(M, cheese)`. Each builder method returns self so calls chain fluently.

## Run

    python3 main.py
