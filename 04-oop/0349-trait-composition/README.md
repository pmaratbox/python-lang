# 0349 — Trait Composition

Compose two capabilities (A printing "a", B printing "b") into one type and invoke both, printing `a b`. Python mixes traits through multiple inheritance, so the composed class inherits both methods.

## Run

    python3 main.py
