# 0347 — Virtual Dispatch

Store a Circle, Square, and Triangle in one Shape collection and call describe() on each, printing `circle square triangle`. In Python every method is virtual, so iterating a base-typed list dispatches to each subclass override automatically.

## Run

    python3 main.py
