# 0151 — Singleton

Obtain a singleton instance twice and confirm both references are the same object, printing `same: yes`. The `is` operator compares object identity, not value.

## Run

    python3 main.py
