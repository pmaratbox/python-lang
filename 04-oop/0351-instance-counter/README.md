# 0351 — Instance Counter

Track how many instances of a class have been created; after building three, print `3`. A class-level attribute incremented in `__init__` is Python's idiomatic shared (static) counter.

## Run

    python3 main.py
