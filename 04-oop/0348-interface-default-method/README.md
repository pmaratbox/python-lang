# 0348 — Interface Default Method

Define an interface with a default greet() returning "hi" and an implementer that overrides it to "hey", printing `hi hey`. Python models a default method as a concrete method on a base class that subclasses may override.

## Run

    python3 main.py
