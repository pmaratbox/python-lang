# 0005 — Loops

Print 1..5 with a for-loop over `range`. Python's `range(start, stop)` is
**half-open** — `stop` is not included, so `range(1, 6)` covers 1..5. A
three-arg form `range(start, stop, step)` controls the step.

## Run

    python3 main.py
