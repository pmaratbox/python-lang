# 0041 — Command-line Arguments

Read the first command-line argument and greet it, so running with `Ada` prints `hello, Ada`. `sys.argv` is the list of arguments; `sys.argv[0]` is the script name, so the first real argument is `sys.argv[1]`. `argparse` builds richer command-line interfaces.

## Run

    python3 main.py Ada
