# 0425 — Create a Stream

Create a stream of 1, 2, 3 with your language's reactive library and print each value, then print done on completion. Uses RxPY (reactivex) with the `rx.of` creation operator and an `on_completed` callback.

## Run

    python3 main.py
