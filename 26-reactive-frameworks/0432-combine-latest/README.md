# 0432 — Combine Latest

Use the library's combineLatest operator on a virtual/test scheduler, emitting the latest pair whenever either timed source emits. Built with RxPY (reactivex) using rx.combine_latest on a TestScheduler.

## Run

    python3 main.py
