# 0429 — Take Operator

Use the library's take operator to take the first 3 values of an unbounded stream, then complete. Built with RxPY (reactivex) using `ops.take(3)` over an unbounded `rx.from_iterable` generator.

## Run

    python3 main.py
