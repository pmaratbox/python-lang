# 0433 — Zip Streams

Use the library's zip operator to pair two streams by index and combine each pair as a+b. Uses RxPY (reactivex) with rx.zip plus ops.map to add the paired tuple elements.

## Run

    python3 main.py
