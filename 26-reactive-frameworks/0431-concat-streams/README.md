# 0431 — Concat Streams

Use the library's concat operator so the second stream starts only after the first completes. Built with RxPY (reactivex) using rx.concat to join two rx.of streams sequentially.

## Run

    python3 main.py
