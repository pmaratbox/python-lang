# 0144 — Flag Enum (Bit Flags)

Combine bit flags READ(1) and WRITE(2) into 3, check that WRITE is set, and print `3 yes`. `enum.IntFlag` supports bitwise `|` and `&` while keeping integer values.

## Run

    python3 main.py
