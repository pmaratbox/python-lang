# 0346 — Newtype Wrapper

Wrap raw integers in distinct UserId and ProductId types so they cannot be confused, printing `user-1 prod-2`. Distinct frozen single-field dataclasses act as newtypes that never mix.

## Run

    python3 main.py
