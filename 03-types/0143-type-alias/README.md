# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. `typing.NewType` makes a distinct semantic alias that is still an `int` at runtime.

## Run

    python3 main.py
