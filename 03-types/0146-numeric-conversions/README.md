# 0146 — Numeric Conversions

Truncate 3.9 to the int 3 and widen the int 3 to the float 3.0, printing `3 3.0`. `int()` truncates toward zero and a `:.1f` format renders the float with one decimal.

## Run

    python3 main.py
