# 0147 — Deep Equality

Compare two nested structures with equal contents for structural equality and print `equal: yes`. Python's `==` on nested tuples compares element-by-element, so it is structural by default.

## Run

    python3 main.py
