# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. Python's Enum preserves declaration order, so `list(Direction)` gives ordinals via index.

## Run

    python3 main.py
