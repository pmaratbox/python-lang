# 0145 — Nested Struct

Model a person whose address is a nested struct, and print the city `London`. A `@dataclass` nests naturally by holding another dataclass as a field.

## Run

    python3 main.py
