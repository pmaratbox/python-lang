# 0085 — Rotate a List

Rotate the list `1, 2, 3, 4, 5` left by `2` positions (elements wrap to the end) and print it: `3 4 5 1 2`. Slicing splits the list at `k` and concatenates the tail before the head, rotating it left by `k`.

## Run

    python3 main.py
