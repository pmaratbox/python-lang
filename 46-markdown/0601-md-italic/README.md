# 0601 — Italic

Uses Python's `markdown` library (Python-Markdown) to render the fixed Markdown input `*italic*` to HTML, producing the emphasis construct `<em>` wrapped in a paragraph. The library appends a trailing newline, which is stripped before printing.

## Run

    python3 main.py
