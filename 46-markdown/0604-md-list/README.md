# 0604 — Unordered list

Uses Python's `markdown` library (Python-Markdown) to render a fixed Markdown unordered list (`- a\n- b`) to HTML, producing a `<ul>` with two `<li>` items, then strips the trailing newline before printing.

## Run

    python3 main.py
