# 0605 — Blockquote

Uses Python's real [`markdown`](https://python-markdown.github.io/) (Python-Markdown) library to render a Markdown blockquote (`> quote`) to HTML, stripping the trailing newline before printing.

## Run

    python3 main.py
