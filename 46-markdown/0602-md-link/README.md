# 0602 — Link

Uses Python's `markdown` library (Python-Markdown) to render a fixed Markdown inline link `[text](http://x.com)` to HTML, producing an `<a href="...">` anchor wrapped in a paragraph, then strips the trailing newline the renderer appends before printing.

## Run

    python3 main.py
