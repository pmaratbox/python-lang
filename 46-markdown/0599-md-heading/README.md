# 0599 — Heading

Uses Python's `markdown` library (Python-Markdown) to render the fixed Markdown heading `# Hello` to HTML, stripping the trailing newline the renderer appends to produce `<h1>Hello</h1>`.

## Run

    python3 main.py
