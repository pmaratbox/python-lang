# 0606 — Ordered list

Render an ordered list with the Python-Markdown library (`markdown`). The input `1. a\n2. b` is converted to an `<ol>` element with two `<li>` items; the renderer's trailing newline is stripped before printing.

## Run

    python3 main.py
