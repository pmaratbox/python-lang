# 0101 — Threads: Spawn and Join

Spawn 3 worker threads, wait for all of them to finish, then print `done: 3`. Python's `threading.Thread.join()` blocks until the target finishes.

## Run

    python3 main.py
