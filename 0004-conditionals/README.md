# 0004 — Conditionals

Compare `n` against 10 and print whether it's less, equal, or greater. Python
uses indentation for blocks, `elif` (one word) for the chained condition, and
no parentheses around the test. Edit `n` to `10` or `15` to exercise the
other branches.

## Run

    python3 main.py
