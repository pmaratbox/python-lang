# 0665 — Big integer addition

Python's built-in `int` is an arbitrary-precision integer, so adding two huge values like `12345678901234567890 + 98765432109876543210` is exact with no overflow and yields `111111111011111111100`.

## Run

    python3 main.py
