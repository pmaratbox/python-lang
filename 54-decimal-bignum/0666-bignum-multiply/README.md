# 0666 — Big integer multiplication

Python's native `int` is arbitrary-precision, so multiplying two large integers (`123456789 * 987654321`) produces the exact product with no overflow or float rounding.

## Run

    python3 main.py
