# 0569 — YAML sequence

Parse a YAML document containing a block sequence (list) using PyYAML's `yaml.safe_load`. The document maps the key `fruits` to a list of three strings; we read that list and join its elements with commas to produce a single plain-text line.

## Run

    python3 main.py
