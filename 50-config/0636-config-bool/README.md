# 0636 — Boolean value

Uses pydantic-settings to load `config.json` into a typed `Settings` model. The
boolean field `debug` is read from the file and printed lowercase (`true`).

## Run

    python3 main.py
