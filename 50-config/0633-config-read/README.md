# 0633 — Read a value

Uses **pydantic-settings** to load `config.json` into a typed `Settings` model and read the top-level string field `name`, which resolves to `myapp`.

## Run

    python3 main.py
