# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. `@staticmethod` defines a method that takes no `self` and is called on the class itself.

## Run

    python3 main.py
