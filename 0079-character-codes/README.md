# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. `ord` returns a character's Unicode code point and `chr` does the inverse; `print` separates the two with a space.

## Run

    python3 main.py
