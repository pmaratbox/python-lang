# 0077 — Title Case

Convert `hello world` to title case (uppercase the first letter of each word) and print it: `Hello World`. `str.split()` breaks on whitespace and `str.capitalize()` uppercases each word's first letter (lowercasing the rest).

## Run

    python3 main.py
