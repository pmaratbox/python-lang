# 0590 — Count matches

This lesson uses **BeautifulSoup (bs4)** with the stdlib `html.parser` to parse a fixed HTML document. It selects every element matching the CSS class selector `.item` with `select(".item")` and prints the number of matches via `len(...)`.

## Run

    python3 main.py
