# 0111 — Partial Application

Partially apply a two-argument add by fixing the first argument to 10, then call the result with 3 to print `13`. Python's `functools.partial` binds one argument and leaves a one-arg function.

## Run

    python3 main.py
