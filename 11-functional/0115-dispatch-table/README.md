# 0115 — Dispatch Table

Store functions in a map keyed by name, then apply "add" and "mul" to (3,4), printing `7 12`. A dict maps names to `operator` functions that you look up and call.

## Run

    python3 main.py
