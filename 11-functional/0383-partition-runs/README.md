# 0383 — Partition Into Runs

Partition [1,1,2,3,3,3] into runs of equal adjacent elements, printing `1 1|2|3 3 3`. `itertools.groupby` groups consecutive equal elements idiomatically.

## Run

    python3 main.py
