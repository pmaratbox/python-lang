# 0386 — Iterate and Take

Iterate f(x)=x*3 from 1 and take the first four values, printing `1 3 9 27`. An infinite generator combined with `itertools.islice` takes a prefix.

## Run

    python3 main.py
