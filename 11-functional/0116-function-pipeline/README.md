# 0116 — Function Pipeline

Compose inc, double, and negate into a left-to-right pipeline and apply it to 3: ((3+1)*2) negated = `-8`. `reduce` threads the value through each function in order.

## Run

    python3 main.py
