# 0613 — Encode null

Using the real `msgpack` library, MessagePack-encode `None` (null/nil) and print the lowercase hex of the resulting bytes: a single byte `c0`, MessagePack's nil format.

## Run

    python3 main.py
