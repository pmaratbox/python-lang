# 0609 — Encode a boolean

Uses the real `msgpack` library to MessagePack-encode the boolean `True` and print the lowercase hex of the resulting bytes. The boolean `true` serializes to the single byte `c3`.

## Run

    python3 main.py
