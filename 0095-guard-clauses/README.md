# 0095 — Guard Clauses

Classify each of `-1`, `0`, and `5` using guard clauses (an early return for each special case) and print `negative`, `zero`, and `positive`. Each special case returns early, so the final `return` handles the remaining case without nested `else` branches.

## Run

    python3 main.py
