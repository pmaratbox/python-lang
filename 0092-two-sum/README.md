# 0092 — Two Sum

Find the two indices in `2, 7, 11, 15` whose values sum to `9` and print them: `0 1`. A dict maps each value to its index; for every number, its needed complement (`target - n`) is looked up in O(1) before the number itself is stored.

## Run

    python3 main.py
