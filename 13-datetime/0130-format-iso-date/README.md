# 0130 — Format ISO Date

Format the date y=2026, m=6, d=4 as a zero-padded ISO string `2026-06-04`. A single `str.format` call with `:02d` width specifiers does the padding.

## Run

    python3 main.py
