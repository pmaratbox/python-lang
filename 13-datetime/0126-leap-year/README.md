# 0126 — Leap Year Check

Determine whether 2000, 1900, and 2024 are leap years, printing `yes no yes`. A generator expression joined with `str.join` keeps the formatting compact.

## Run

    python3 main.py
