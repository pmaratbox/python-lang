# 0091 — Balanced Parentheses

Using a stack, check whether `(())` is balanced and whether `(()` is balanced, printing `yes` then `no`. Each `(` is pushed and each `)` pops a match; an unmatched `)` or a non-empty stack at the end means unbalanced.

## Run

    python3 main.py
