# 0002 — Variables & basic types

Declare bindings of the four basic types (`int`, `float`, `str`, `bool`) and
print each one. Type annotations are optional in Python — they're shown here
to make the intent explicit. Python prints booleans with a capital `True`/
`False`, which is the language convention.

## Run

    python3 main.py
