# 0075 — Anagram Check

Check whether each pair is an anagram by comparing sorted letters — `listen`/`silent` and `hello`/`world` — printing `listen/silent: yes` and `hello/world: no`. `sorted(word)` returns the letters in order; two words are anagrams exactly when their sorted letter lists are equal.

## Run

    python3 main.py
