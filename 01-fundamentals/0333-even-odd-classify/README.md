# 0333 — Classify Even/Odd

Classify each of [1,2,3,4] as odd or even, printing `odd even odd even`. A list comprehension with a conditional expression maps each number to its label.

## Run

    python3 main.py
