# 0088 — String Builder

Use a string builder to assemble `1`, `2`, and `3` joined by `-`, printing `1-2-3`. Python strings are immutable, so `io.StringIO` is the in-memory builder you `write` into (joining a list also works).

## Run

    python3 main.py
