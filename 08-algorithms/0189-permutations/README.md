# 0189 — Permutations

Generate all permutations of [1,2,3] in lexicographic order, one per line space-separated. `itertools.permutations` yields them in lexicographic order for sorted input.

## Run

    python3 main.py
