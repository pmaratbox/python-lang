# 0071 — Celsius to Fahrenheit

Convert `100` and `0` degrees Celsius to Fahrenheit using `F = C * 9 / 5 + 32`, printing `100C = 212F` and `0C = 32F`. `//` is floor division, keeping the result an `int`; the f-string formats each `C = F` line.

## Run

    python3 main.py
