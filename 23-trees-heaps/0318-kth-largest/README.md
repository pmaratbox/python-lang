# 0318 — Kth Largest

Find the 2nd largest element of [3,2,1,5,6,4] using a heap, printing `5`. A size-k min-heap retains the k largest values seen, with its root being the answer.

## Run

    python3 main.py
