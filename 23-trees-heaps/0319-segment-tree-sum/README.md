# 0319 — Segment Tree Range Sum

Build a segment tree over [1,2,3,4,5] and query the sum of indices 1..3 (inclusive), printing `9`. The iterative array-backed tree stores leaves in the second half and folds the range upward.

## Run

    python3 main.py
