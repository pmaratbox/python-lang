# 0315 — Tree Diameter

Compute the diameter (longest path in edges) of a tree where root has children A and B, and A has children C and D, printing `3`. The post-order recursion tracks the two tallest child heights at each node to update a running best.

## Run

    python3 main.py
