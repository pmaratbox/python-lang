# 0309 — Validate BST

Check whether two trees are valid BSTs: a correct one (yes) and one with an out-of-place node (no), printing `yes no`. The recursive bound check uses `float("-inf")` and `float("inf")` as sentinels.

## Run

    python3 main.py
