# 0317 — Heap Sort

Heap-sort [5,3,8,1,4] ascending, printing `1 3 4 5 8`. The in-place sort builds a max-heap with `sift_down`, then repeatedly swaps the root to the end and re-heapifies.

## Run

    python3 main.py
