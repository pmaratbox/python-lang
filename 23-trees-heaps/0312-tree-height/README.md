# 0312 — Tree Height

Compute the height (in nodes) of the BST built from 5,3,8,1,4, printing `3`. The recursion returns `1 + max(left, right)` with an empty subtree counting as zero.

## Run

    python3 main.py
