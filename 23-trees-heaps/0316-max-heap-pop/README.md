# 0316 — Max-Heap Pop All

Build a max-heap from [3,1,4,1,5], pop the top three values, printing `5 4 3`. Python's `heapq` is a min-heap, so values are negated to emulate a max-heap.

## Run

    python3 main.py
