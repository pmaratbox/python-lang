# 0687 — Zip

Uses the [more-itertools](https://more-itertools.readthedocs.io/) library's `zip_broadcast` transform to pair `[1,2,3]` with `["a","b","c"]` element-wise, formatting each pair as `<n><s>` and comma-joining the results.

## Run

    python3 main.py
