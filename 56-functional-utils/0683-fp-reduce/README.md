# 0683 — Reduce

Reduce (fold) the list `[1,2,3,4,5]` to a single value using `functools.reduce` from Python's standard library, accumulating with addition starting from `0` -> `15`.

## Run

    python3 main.py
