# 0689 — Take

Python's `toolz` library takes the first 3 elements of `[1..10]` with `toolz.take`, which lazily yields the leading `n` items of any iterable; the result is comma-joined to produce `1,2,3`.

## Run

    python3 main.py
