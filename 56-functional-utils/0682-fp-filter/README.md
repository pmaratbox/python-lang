# 0682 — Filter

Filter even numbers from `[1,2,3,4,5,6]` using `toolz.filter` (the toolz functional library's lazy filter) with an even predicate, then comma-join the result to get `2,4,6`.

## Run

    python3 main.py
