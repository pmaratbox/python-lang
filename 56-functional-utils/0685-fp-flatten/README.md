# 0685 — Flatten

Uses the [toolz](https://toolz.readthedocs.io/) functional library and its `toolz.concat` transform to flatten the list of lists `[[1,2],[3,4],[5,6]]` one level into a single lazy iterator, then comma-joins the elements into `1,2,3,4,5,6`.

## Run

    python3 main.py
