# 0686 — Unique

Remove duplicates preserving order. Uses `toolz.unique`, the deduplication transform from the `toolz` functional library, which yields each element the first time it is seen and skips later repeats.

## Run

    python3 main.py
