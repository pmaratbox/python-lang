# 0684 — Chunk

Split a list into fixed-size pieces with `more_itertools.chunked`. Chunking `[1,2,3,4,5,6]` into size-2 groups, then joining each chunk with `,` and the chunks with `|`, gives `1,2|3,4|5,6`.

## Run

    python3 main.py
