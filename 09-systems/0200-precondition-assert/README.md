# 0200 — Precondition Assert

Check a precondition arg>0: for 5 print `ok`, and for -1 report the failure `error: must be positive`, on two lines. A guard clause raises `ValueError`, which the caller catches to report the failure message.

## Run

    python3 main.py
