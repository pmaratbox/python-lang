# 0373 — Exception Hierarchy

Throw a specific error subtype and catch it through a base-type handler, printing `caught base`. In Python an `except` clause matches any subclass of the named exception.

## Run

    python3 main.py
