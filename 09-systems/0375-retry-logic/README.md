# 0375 — Retry With Attempts

Retry an operation that fails on attempts 1 and 2 and succeeds on attempt 3, printing `ok after 3`. A `for` loop with `break` stops as soon as the operation succeeds.

## Run

    python3 main.py
