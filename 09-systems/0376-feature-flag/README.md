# 0376 — Feature Flag

Branch on a boolean feature flag: on -> "enabled", off -> "disabled", printing `enabled disabled`. A conditional expression keeps the same code path for both flag values.

## Run

    python3 main.py
