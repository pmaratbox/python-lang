# 0194 — Try / Catch / Finally

Throw and catch an error, printing `caught`, and always run a finally block printing `cleanup`, on two lines. Python uses `try`/`except`/`finally`, where the `finally` block always runs.

## Run

    python3 main.py
