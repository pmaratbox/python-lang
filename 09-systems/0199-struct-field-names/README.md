# 0199 — Struct Field Names

List the field names of a Point{x,y} struct/record and print `x y`. A `@dataclass` exposes its declared fields via `dataclasses.fields`, preserving declaration order.

## Run

    python3 main.py
