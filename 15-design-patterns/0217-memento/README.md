# 0217 — Memento

Save state 1, change it to 2, then restore from the memento, printing current then restored `2 1`. The memento captures a snapshot the originator can later restore.

## Run

    python3 main.py
