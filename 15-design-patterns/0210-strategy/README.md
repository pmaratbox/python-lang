# 0210 — Strategy

Select an "add" then a "mul" strategy and apply each to (3,4), printing `7 12`. Plain functions are first-class, so they serve as interchangeable strategies.

## Run

    python3 main.py
