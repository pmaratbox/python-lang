# 0213 — Iterator (Custom)

Implement a custom iterator over a range 1..3 and consume it, printing `1 2 3`. Python's iterator protocol is `__iter__`/`__next__` with `StopIteration`.

## Run

    python3 main.py
