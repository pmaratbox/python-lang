# 0209 — Observer

Notify two observers of a new value 5; each prints its id and the value on its own line. The subject keeps a list and broadcasts to each registered observer.

## Run

    python3 main.py
