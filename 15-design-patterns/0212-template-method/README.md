# 0212 — Template Method

Run an algorithm skeleton whose middle step is overridden, printing `start work end` on one line. The base `run()` fixes the skeleton; the subclass supplies only `step()`.

## Run

    python3 main.py
