# 0205 — Decorator

Decorate a base coffee (cost 2) with milk (+1) and sugar (+1), printing the total cost `4`. Each wrapper forwards to the component and adds its own cost.

## Run

    python3 main.py
