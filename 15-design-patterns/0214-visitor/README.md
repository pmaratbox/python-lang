# 0214 — Visitor

Use a visitor to sum the values of a small node tree with leaves 1, 2, 3, printing `6`. Each node's `accept` calls back the matching `visit_*` method (double dispatch).

## Run

    python3 main.py
