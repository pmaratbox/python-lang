# 0083 — Sliding Window

Slide a window of size `2` over `1, 2, 3, 4` and print each window on its own line: `1 2`, `2 3`, `3 4`. Each start index from `0` to `len - size` produces a window via the slice `nums[i:i+size]`.

## Run

    python3 main.py
