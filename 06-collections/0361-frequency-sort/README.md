# 0361 — Frequency Sort

Sort [1,1,2,3,3,3] by descending frequency (ties keep first-seen order), printing `3 3 3 1 1 2`. Counter preserves insertion order and `sorted` is stable, so a descending count sort keeps first-seen ties.

## Run

    python3 main.py
