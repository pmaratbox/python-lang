# 0165 — All and Any

For [2,4,6] report whether all are even (yes) and whether any are odd (no), printing `yes no`. Use the built-in all() and any() over generator expressions.

## Run

    python3 main.py
