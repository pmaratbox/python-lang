# 0055 — Frequency Count

Count how many times each letter appears in `banana` and print the per-letter counts in alphabetical order: `a:3 b:1 n:2`. `collections.Counter` tallies hashable items in one pass; `sorted(counts)` iterates its keys in alphabetical order for the output.

## Run

    python3 main.py
