# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. `ch in vowels` tests membership in the vowel string; `sum(1 for ...)` counts the matches in one pass.

## Run

    python3 main.py
