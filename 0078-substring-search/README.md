# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. `str.index` returns the starting position of the first match (and raises if absent, unlike `str.find`, which returns `-1`).

## Run

    python3 main.py
