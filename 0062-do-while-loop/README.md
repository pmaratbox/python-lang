# 0062 — Do-While Loop

Use a do-while loop (whose body runs once before the condition is checked) to print `1`, `2`, and `3` on one line: `1 2 3`. Python has no do-while; `while True:` with a `break` at the bottom of the body runs the body once before the condition is tested.

## Run

    python3 main.py
