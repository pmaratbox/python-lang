# 0120 — Append to a File

Write "a" to a file, append "b", then read both lines back and print `a b`. Opening with mode "a" appends without truncating the existing contents.

## Run

    python3 main.py
