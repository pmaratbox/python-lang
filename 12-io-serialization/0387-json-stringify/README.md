# 0387 — JSON Stringify

Serialize the object {name:"Ada", age:36} to compact JSON `{"name":"Ada","age":36}`. Use `json.dumps` with `separators=(",", ":")` to drop the default spaces.

## Run

    python3 main.py
