# 0121 — Parse CSV

Parse the two CSV rows `alice,30` and `bob,25` into name=value pairs and print `alice=30 bob=25`. str.split(",") turns each row into its comma-separated fields.

## Run

    python3 main.py
