# 0125 — Temp File Roundtrip

Write a string to a temporary file, read it back, confirm it matches, delete the file, and print `roundtrip: ok`. tempfile.mkstemp creates a uniquely named file and returns an open descriptor plus its path.

## Run

    python3 main.py
