# 0390 — Big-Endian Bytes

Encode the integer 258 as two big-endian bytes (1, 2), decode them back to 258, printing `1 2 258`. `int.to_bytes`/`int.from_bytes` handle big-endian conversion directly.

## Run

    python3 main.py
