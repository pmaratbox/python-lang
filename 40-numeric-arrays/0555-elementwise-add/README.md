# 0555 — Element-wise add

Uses NumPy's element-wise addition: two `np.array` vectors are summed with the `+` operator, which adds matching positions. The integer result is printed as space-joined values.

## Run

    python3 main.py
