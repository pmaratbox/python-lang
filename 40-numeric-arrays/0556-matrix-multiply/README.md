# 0556 — Matrix multiply

Multiply two 2x2 matrices with numpy. `np.array` builds each matrix and the `@` operator performs matrix multiplication (matmul); each result row is printed as space-joined integers.

## Run

    python3 main.py
