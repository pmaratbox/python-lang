# 0553 — Vector sum

Builds the vector `[1, 2, 3, 4]` with NumPy and reduces it to a scalar total using the library's `ndarray.sum()`. The result is cast to an integer so the output has no decimal point.

## Run

    python3 main.py
