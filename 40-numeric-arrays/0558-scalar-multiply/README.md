# 0558 — Scalar multiply

Build the matrix `[[1, 2], [3, 4]]` with NumPy's `np.array` and multiply it by the scalar `3` using broadcasted scalar multiplication (`scalar * a`), which scales every element. Each resulting row is printed as space-joined integers, one row per line.

## Run

    python3 main.py
