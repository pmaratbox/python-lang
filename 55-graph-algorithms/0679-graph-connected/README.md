# 0679 — Connectivity

Python's `networkx` library builds the fixed weighted undirected graph G and uses `nx.has_path` to determine whether any path connects nodes `a` and `e` (reachability). Since `a-b-c-d-e` exists, the nodes are connected, so the result is `true`.

## Run

    python3 main.py
