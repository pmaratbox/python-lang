# 0673 — Build a graph

Python's `networkx` library builds the fixed weighted undirected graph G (nodes a–e, six weighted edges) with `nx.Graph().add_weighted_edges_from`, then reports its size via `number_of_nodes()` and `number_of_edges()`, printing `5 6`.

## Run

    python3 main.py
