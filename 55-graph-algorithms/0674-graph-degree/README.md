# 0674 — Node degree

Builds the fixed weighted undirected graph G with [networkx](https://networkx.org/) and queries `Graph.degree(node)`, which returns the number of edges incident to a node. Node `b` connects to `a`, `c`, and `d`, so its degree is `3`.

## Run

    python3 main.py
