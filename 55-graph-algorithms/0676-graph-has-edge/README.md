# 0676 — Edge existence

Uses Python's `networkx` graph library to build the fixed weighted undirected graph `G` and queries `Graph.has_edge`, which returns whether a given pair of nodes is directly connected by an edge. We check `b-c` (present) and `a-e` (absent), printing the booleans lowercase and space-joined.

## Run

    python3 main.py
