# 0678 — Shortest path route

Uses the [NetworkX](https://networkx.org/) graph library to build a fixed weighted undirected graph `G` and compute the unique weighted shortest path from `a` to `e` with `nx.shortest_path(..., weight="weight")` (Dijkstra's algorithm), joining the node sequence with `-`.

## Run

    python3 main.py
