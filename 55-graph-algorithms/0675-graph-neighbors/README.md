# 0675 — Neighbors

Uses Python's `networkx` graph library to build the fixed weighted undirected graph G and list the neighbors of node `a`. `G.neighbors("a")` returns the adjacent nodes from the library's adjacency structure; sorting them gives a deterministic comma-joined result.

## Run

    python3 main.py
