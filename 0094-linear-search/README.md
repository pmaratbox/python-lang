# 0094 — Linear Search

Linearly scan the array `4, 2, 7, 1, 9` for `7` and print the index where it is found: `found 7 at index 2`. A linear scan checks each element in turn until it matches; unlike binary search, it needs no sorted input.

## Run

    python3 main.py
