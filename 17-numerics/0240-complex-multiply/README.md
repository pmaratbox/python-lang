# 0240 — Complex Multiplication

Multiply the complex numbers (1+2i) and (3+4i), printing the real and imaginary parts `-5 10`. Python has a built-in complex type, so the multiplication is just the `*` operator.

## Run

    python3 main.py
