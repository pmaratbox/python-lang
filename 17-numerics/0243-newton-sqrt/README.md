# 0243 — Newton Square Root

Approximate sqrt(2) with Newton iteration and print it to 4 decimals `1.4142`. We iterate until the estimate stabilizes, then format with an f-style `{:.4f}` spec.

## Run

    python3 main.py
