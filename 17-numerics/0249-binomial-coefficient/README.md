# 0249 — Binomial Coefficient

Compute C(5,2) without overflow and print `10`. The multiplicative formula with integer floor division stays exact; Python 3.8+ also offers `math.comb`.

## Run

    python3 main.py
