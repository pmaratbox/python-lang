# 0268 — Cellular Automaton Rule 90

Apply one Rule 90 step (new = left XOR right) to the row "00100" with zero boundaries, printing `01010`. Out-of-range neighbors default to 0 via index guards.

## Run

    python3 main.py
