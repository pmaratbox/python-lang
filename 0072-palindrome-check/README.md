# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. `word[::-1]` reverses the string with a step of `-1`; comparing it to the original tells whether the word is a palindrome.

## Run

    python3 main.py
