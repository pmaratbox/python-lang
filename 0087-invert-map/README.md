# 0087 — Invert a Map

Invert the map `{a: 1, b: 2, c: 3}` (swap keys and values) and print the result sorted by the new key: `1:a 2:b 3:c`. A dict comprehension swaps each `k: v` into `v: k`; `sorted(inverted)` orders the new integer keys for output.

## Run

    python3 main.py
