# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. A `sorted` call with a slice key expresses this in one line.

## Run

    python3 main.py
