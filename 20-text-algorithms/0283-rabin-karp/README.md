# 0283 — Rabin-Karp Search

Use rolling-hash Rabin-Karp to find all start indices of "ab" in "xabxab", printing `1 4`. Python's `pow(base, k, mod)` gives the modular high coefficient directly.

## Run

    python3 main.py
