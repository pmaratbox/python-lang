# 0006 — Functions

Define `add(a, b)` returning their sum, call it with `3` and `4`, and print
the result. `def` introduces a function; `return` yields its value. The
type hints (`: int`, `-> int`) are optional documentation — Python does not
enforce them at runtime.

## Run

    python3 main.py
