# 0061 — Reverse a String

Reverse the string `abc` character by character and print the result: `cba`. Slicing with a step of `-1` (`text[::-1]`) returns the string reversed, walking its code points back to front.

## Run

    python3 main.py
