# 0302 — Kruskal MST

Compute the MST total weight of edges (0,1,1),(1,2,2),(0,2,3) with union-find, printing `3`. `sorted` with a key lambda orders edges, and path-halving keeps `find` flat.

## Run

    python3 main.py
