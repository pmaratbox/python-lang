# 0565 — Base64 encode

Uses Python's standard-library `base64` module to Base64-encode the UTF-8 bytes of `"hello"`. `base64.b64encode` returns the encoded value as `bytes`, which is decoded to an ASCII `str` for printing.

## Run

    python3 main.py
