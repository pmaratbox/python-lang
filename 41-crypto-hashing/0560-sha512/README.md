# 0560 — SHA-512

Computes the SHA-512 hash of the fixed string `hello` using Python's standard-library `hashlib` module and prints the lowercase hexadecimal digest via `hexdigest()`. SHA-512 is deterministic, so the same input always yields the same 128-character hex string.

## Run

    python3 main.py
