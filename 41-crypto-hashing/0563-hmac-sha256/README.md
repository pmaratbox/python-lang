# 0563 — HMAC-SHA256

Uses Python's standard-library `hmac` module together with `hashlib` to compute an HMAC-SHA256 message authentication code over the fixed message `hello` keyed with `key`, then prints the result as a lowercase hex string via `hexdigest()`.

## Run

    python3 main.py
