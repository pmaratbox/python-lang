# 0559 — SHA-256

Compute the SHA-256 of the UTF-8 bytes of `hello` and print the lowercase hex digest. This uses the standard library's `hashlib` module, which provides the SHA-256 algorithm; `.hexdigest()` returns the 32-byte digest as a lowercase hex string with no separators.

## Run

    python3 main.py
