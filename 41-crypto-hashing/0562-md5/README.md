# 0562 — MD5

Computes the MD5 digest of the UTF-8 bytes of `"hello"` using Python's standard-library `hashlib` module (`hashlib.md5`) and prints the lowercase hex digest with `.hexdigest()`. MD5 is cryptographically broken and shown here only as a legacy algorithm.

## Run

    python3 main.py
