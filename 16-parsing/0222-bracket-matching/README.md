# 0222 — Bracket Matching

Check that mixed brackets are balanced: "([{}])" (yes) and "([)]" (no), printing `yes no`. A list used as a stack with a closer-to-opener dict keeps the matcher tidy.

## Run

    python3 main.py
