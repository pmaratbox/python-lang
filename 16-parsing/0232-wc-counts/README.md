# 0232 — Word/Line/Char Counts

For the text "a b\nc" report words, lines, and characters, printing `3 2 5`. `str.split()` with no argument splits on any whitespace to count words.

## Run

    python3 main.py
