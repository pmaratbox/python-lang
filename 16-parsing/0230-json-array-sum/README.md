# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. A generator expression feeds `sum` directly after slicing off the brackets.

## Run

    python3 main.py
