# 0231 — Template Substitution

Substitute the variable in the template "hi {name}" with name="Ada", printing `hi Ada`. `re.sub` with a callback replaces each `{key}` from the values map.

## Run

    python3 main.py
