# 0234 — Parse Number Bases

Parse "ff" as hexadecimal (255) and "101" as binary (5), printing `255 5`. Python's built-in `int(s, base)` does the base conversion directly.

## Run

    python3 main.py
