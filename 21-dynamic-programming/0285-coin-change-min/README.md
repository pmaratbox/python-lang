# 0285 — Coin Change (Min Coins)

Find the fewest coins from {1,2,5} summing to 11, printing `3`. A bottom-up list `dp` with `float("inf")` sentinels keeps the code idiomatic.

## Run

    python3 main.py
