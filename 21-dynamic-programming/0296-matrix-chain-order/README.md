# 0296 — Matrix Chain Order

Find the minimum scalar multiplications to multiply matrices with dimensions [10,20,30,40], printing `18000`. A generator inside `min` scans every split point cleanly.

## Run

    python3 main.py
