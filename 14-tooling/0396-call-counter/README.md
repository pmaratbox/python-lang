# 0396 — Call Counter

Wrap a function so each call increments a counter; after five calls print `calls: 5`. A decorator stores the count as an attribute on the wrapper function.

## Run

    python3 main.py
