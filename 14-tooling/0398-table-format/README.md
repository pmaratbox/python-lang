# 0398 — Aligned Table

Format the rows (a,1),(bb,22) with the first column left-padded to the widest value, printing two aligned rows. str.ljust pads each first cell to the computed max width.

## Run

    python3 main.py
