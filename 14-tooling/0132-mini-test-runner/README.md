# 0132 — Mini Test Runner

Run three named test functions that all pass and report `3 passed, 0 failed`. Each test is a plain function collected in a list and run in a try/except that tallies results.

## Run

    python3 main.py
