# 0397 — Log Level Filter

With a threshold of WARN, log messages at INFO, WARN, and ERROR but only emit WARN and ERROR, on two lines. A dict maps level names to numeric ranks for comparison.

## Run

    python3 main.py
