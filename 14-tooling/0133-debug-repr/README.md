# 0133 — Debug Representation

Produce a debug/structured representation of a Point(1,2) and print `Point(x=1, y=2)`. Defining `__repr__` gives the type its developer-facing string.

## Run

    python3 main.py
