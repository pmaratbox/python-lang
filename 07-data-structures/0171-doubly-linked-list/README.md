# 0171 — Doubly Linked List

Build a doubly linked list 1<->2<->3, traverse forward then backward, printing `1 2 3` then `3 2 1` on two lines. Each node holds `prev`/`next` references and we walk head->tail then tail->head.

## Run

    python3 main.py
