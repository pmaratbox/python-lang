# 0178 — Stack With Min

Build a stack that tracks its minimum in O(1); after pushing 3,1,2 print `min: 1`. An auxiliary min-stack mirrors each push so `get_min` is just the top of `mins`.

## Run

    python3 main.py
