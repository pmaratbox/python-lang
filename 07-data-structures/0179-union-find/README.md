# 0179 — Union-Find

Union (0,1) and (2,3), then query connectivity of (0,1)=yes and (0,2)=no, printing `yes no`. A disjoint-set keeps a `parent` array with path-halving in `find` and root linking in `union`.

## Run

    python3 main.py
