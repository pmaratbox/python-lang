# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. An array-backed heap uses sift-up on push and sift-down on pop, with tuple-swap for the exchanges.

## Run

    python3 main.py
