# 0365 — Multiset Counts

Add 1,1,2 to a multiset; print the count of 1 (2), remove one 1, then print the count of 1 (1), as `2 1`. collections.Counter maps each element to its multiplicity, defaulting missing keys to 0.

## Run

    python3 main.py
