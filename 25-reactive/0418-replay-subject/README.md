# 0418 — ReplaySubject

Implement a ReplaySubject with a buffer of the last 2 values, replayed to a late subscriber, which then also receives new values. A list slice keeps the buffer trimmed to the most recent two values.

## Run

    python3 main.py
