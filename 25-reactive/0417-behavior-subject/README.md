# 0417 — BehaviorSubject

Implement a BehaviorSubject that holds a current value and replays it immediately to each new subscriber. The subject stores `current` and pushes it to each subscriber's `on_next` closure the moment they subscribe.

## Run

    python3 main.py
