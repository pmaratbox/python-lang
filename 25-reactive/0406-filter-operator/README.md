# 0406 — Filter Operator

Implement a filter operator that forwards only values passing a predicate, keeping the even numbers of 1..6. The operator wraps the source observer in an inner observer that gates each value behind a lambda predicate.

## Run

    python3 main.py
