# 0410 — Merge Streams

Implement merge of two timed streams using a virtual-time scheduler, interleaving them by emission time. Uses Python's `heapq` module as the scheduler's priority queue, with an insertion sequence to break ties at equal times.

## Run

    python3 main.py
