# 0413 — Zip Streams

Implement zip that pairs values by index and combines them; zip [1,2,3] with [10,20,30] using a+b. Each source buffers into a per-source list and drains a pair whenever both lists are non-empty.

## Run

    python3 main.py
