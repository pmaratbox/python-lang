# 0259 — Enumerate Submasks

Enumerate all submasks of the mask 5 (101) in descending order `5 4 1 0`. A `while True` with an explicit break captures the final 0 before the mask wraps.

## Run

    python3 main.py
